{
  "name": "saas-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --delay 200"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
