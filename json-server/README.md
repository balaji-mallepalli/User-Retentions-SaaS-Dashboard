# JSON Server for SaaS Demo

This folder provides a lightweight JSON API using `json-server` for local development and CRUD demos.

Start it with:

```powershell
cd json-server
npm install
npm start
```

The server will run on `http://localhost:3001` and expose endpoints like `/items`, `/users`, `/notes`.

Use this for client-side CRUD demos or simple prototyping.
